config display manager
    $ sudo dnf install gdm
    $ sudo systemctl disable sddm
    $ sudo systemctl enable gdm

windows tile
    $ sudo dnf install qtile
        set in config window to qtile
        $ sudo reboot

reinstall screen lock
    $ sudo dnf install lxlock
